<div id="<%= dom_id category %>">
  <p>
    <%= category.name %>
  </p>
  <%= link_to 'Edit', edit_category_path(category) %>
  <%= link_to 'Delete', category_path(category), data: {turbo_method: :delete, turbo_confirm: 'Estas seguro' } %>
</div>
